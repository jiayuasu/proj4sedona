name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  datum-registry:
    name: Verify generated datum registry
    runs-on: ubuntu-latest
    env:
      PROJ4JS_DATUM_COMMIT: 955bfd6bda76982bba87d683990b3ae3393a7e0b

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Checkout pinned proj4js source
      uses: actions/checkout@v7
      with:
        repository: proj4js/proj4js
        ref: ${{ env.PROJ4JS_DATUM_COMMIT }}
        path: .upstream/proj4js
        persist-credentials: false

    - name: Set up Node.js
      uses: actions/setup-node@v7
      with:
        node-version: '24'
        package-manager-cache: false

    - name: Check generated datum registry
      run: node scripts/sync-proj4js-datums.mjs --check .upstream/proj4js

  esri-aliases:
    name: Verify generated Esri name index
    runs-on: ubuntu-latest
    env:
      ESRI_DB_DOC_COMMIT: 06dc50b556a2fa49224453ce8cbcf1833b3b64f7

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Checkout pinned Esri projection engine database documentation
      uses: actions/checkout@v7
      with:
        repository: Esri/projection-engine-db-doc
        ref: ${{ env.ESRI_DB_DOC_COMMIT }}
        path: .upstream/projection-engine-db-doc
        persist-credentials: false

    - name: Check generated Esri name index
      run: python3 scripts/sync-esri-aliases.py --check .upstream/projection-engine-db-doc

  test:
    name: Test on Java ${{ matrix.java }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        java: ['11', '17', '21']
      fail-fast: false

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Set up JDK ${{ matrix.java }}
      uses: actions/setup-java@v6
      with:
        java-version: ${{ matrix.java }}
        distribution: 'temurin'
        cache: 'maven'

    - name: Cache Maven packages
      uses: actions/cache@v6
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-

    - name: Build and test with Maven
      run: mvn clean install -B

    - name: Generate test report
      if: always()
      run: mvn surefire-report:report-only

    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v7
      with:
        name: test-results-java-${{ matrix.java }}
        path: |
          **/target/surefire-reports/*.xml
          **/target/surefire-reports/*.txt
        retention-days: 30

  package:
    name: Package
    runs-on: ubuntu-latest
    needs: test

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Set up JDK 11
      uses: actions/setup-java@v6
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: 'maven'

    - name: Build and package
      run: mvn clean package -DskipTests -B

    - name: Upload JAR artifacts
      uses: actions/upload-artifact@v7
      with:
        name: maven-artifacts
        path: target/*.jar
        retention-days: 30

  code-quality:
    name: Code Quality Checks
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Set up JDK 11
      uses: actions/setup-java@v6
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: 'maven'

    - name: Install dependencies
      run: mvn clean install -DskipTests -B

    - name: Generate Javadoc
      run: mvn javadoc:javadoc -B

  multi-os:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
      fail-fast: false

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Set up JDK 11
      uses: actions/setup-java@v6
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: 'maven'

    - name: Build and test
      run: mvn clean test -B
